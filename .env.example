# Gemini API Key (Backend only - Railway)
# Get your API key from: https://makersuite.google.com/app/apikey
# Only needed for Railway backend - NOT for Vercel frontend
GEMINI_API_KEY=your_gemini_api_key_here

# Google Search API (for function calling - stock prices, weather, web search)
# Setup instructions in README.md
GOOGLE_SEARCH_API_KEY=your_google_search_api_key_here
GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id_here

# Google OAuth (for user authentication)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID with authorized redirect URIs:
#   - Development: http://localhost:3000/api/auth/callback/google
#   - Production: https://your-app.railway.app/api/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# NextAuth Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_here_at_least_32_characters
NEXTAUTH_URL=http://localhost:3000  # Change to production URL when deploying

# Rate Limiting (Optional - Very generous defaults)
# Prevents abuse while allowing extensive testing and demos
# Defaults: 60 per minute, 500 per hour
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=500

# Monitoring & Error Tracking (Optional but recommended for production)
# Get your Sentry DSN from: https://sentry.io
SENTRY_DSN=your_sentry_dsn_here
NEXT_PUBLIC_SENTRY_DSN=your_sentry_client_dsn_here

# Proxy Configuration (Required for production deployments)
# Set to 'true' when deploying behind Railway, Vercel, or other reverse proxies
# This enables proper rate limiting based on client IP
# IMPORTANT: Only set to 'true' when behind a TRUSTED proxy
TRUST_PROXY=false
